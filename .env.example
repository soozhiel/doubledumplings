# Real form delivery remains disabled until both values are configured in Vercel.
RESEND_API_KEY=
PRIVATE_BRIEF_TO_EMAIL=
# Optional; defaults to onboarding@resend.dev until a Caelverum sending domain is verified.
PRIVATE_BRIEF_FROM_EMAIL=
